<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_setting_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    >

    <ImageView
        android:id="@+id/iv_start_icon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_gravity="center"
        android:visibility="gone"
        android:layout_marginEnd="@dimen/dp_8"
        tools:visibility="visible"
        tools:src="@drawable/ic_help_feedback" />

    <TextView
        android:id="@+id/tv_start_text"
        style="@style/t16_l"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:paddingTop="16dp"
        android:paddingEnd="16dp"
        android:paddingBottom="16dp"
        android:textColor="?sc_text_dark"
        tools:text="配件服务" />

    <ImageView
        android:id="@+id/iv_red_dot"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_gravity="center_vertical"
        android:src="@drawable/common_red_dot"
        android:visibility="gone"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/tv_tip_text"
        style="@style/t14_l"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:drawableEnd="@drawable/ic_icon_outline_right2"
        android:drawablePadding="4dp"
        android:textColor="?sc_text_grey"
        tools:text="新" />
</LinearLayout>
